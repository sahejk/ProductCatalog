platform :ios, '9.0'
use_frameworks!

target 'ProductCatalog' do
  # Pods for ProductCatalog
  pod 'Alamofire', '4.3.0'
  pod 'Kingfisher', '~> 3.0'
  pod 'M13BadgeView'
  target 'ProductCatalogTests' do
    inherit! :search_paths
    # Pods for testing
    
  end

end
